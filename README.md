# X聚合信息

> 信息聚合与开源项目展示平台

## 🎯 项目介绍

X聚合信息是一个轻量级的个人信息展示平台，用于整合展示：

- 📱 个人社交媒体信息（CSDN、微信公众号、GitHub等）
- 📦 开源项目集合展示
- 📰 科技日报推送（按日期归档）
- 💬 访客留言评论系统

## ✨ 功能特性

- **响应式设计** - 完美适配PC端和移动端
- **Markdown支持** - 实时渲染MD文档内容
- **日期归档** - 咨询文档按 `YYYY/MM/DD/` 目录结构自动组织
- **配置化管理** - 所有关键信息通过 `config.json` 管理
- **评论系统** - 内置简易留言板功能
- **单页应用** - 无刷新切换页面，体验流畅

## 🚀 快速开始

### 环境要求

- Python 3.8+
- pip

### 安装步骤

1. 克隆仓库

```bash
git clone https://github.com/Ajie16/x-message.git
cd x-message
```

2. 安装依赖

```bash
pip install -r requirements.txt
```

3. 配置站点信息

编辑 `config.json` 文件，修改个人信息：

```json
{
  "author": {
    "name": "你的名称",
    "bio": "个人简介",
    "followers": {
      "csdn": 1000,
      "wechat": 2000,
      "github": 500
    }
  },
  "links": {
    "csdn": {
      "name": "CSDN博客",
      "url": "https://blog.csdn.net/your_username",
      "show": true
    }
  }
}
```

4. 启动服务

```bash
python app.py
```

5. 访问 http://localhost:8081

## 📁 项目结构

```
x-message/
├── app.py                   # Flask后端主程序
├── config.json              # 全局配置文件
├── requirements.txt         # Python依赖
├── README.md                # 项目说明
├── .gitignore              # Git忽略配置
├── static/                  # 静态资源
│   ├── css/
│   │   └── style.css       # 样式文件
│   ├── js/
│   │   └── main.js         # 前端脚本
│   └── images/             # 图片资源
├── templates/
│   └── index.html          # 主页面模板
├── content/                 # 内容文档
│   ├── projects/           # 开源项目MD文档
│   └── news/               # 科技日报（按日期目录 YYYY/MM/DD/）
└── data/
    └── comments.json       # 评论数据
```

## 📝 内容管理

### 开源项目文档

将 Markdown 文件放入 `content/projects/` 目录：

```bash
content/projects/
├── project-a.md
├── project-b.md
└── awesome-tool.md
```

### 科技日报文档

按日期目录结构存放：

```bash
content/news/
└── 2026/
    └── 02/
        ├── 22/
        │   └── hello-world.md
        └── 23/
            └── weekly-update-01.md
```

## ⚙️ 配置说明

### config.json 详解

```json
{
  "site": {
    "name": "网站名称",
    "description": "网站描述",
    "port": 8081,              // 服务端口
    "host": "0.0.0.0"          // 监听地址
  },
  "author": {
    "name": "作者名称",
    "bio": "个人简介",
    "followers": {             // 粉丝数量显示
      "csdn": 1000,
      "wechat": 2000,
      "github": 500
    }
  },
  "links": {                   // 社交链接
    "csdn": {
      "name": "CSDN博客",
      "url": "链接地址",
      "show": true             // 是否显示
    }
  },
  "comments": {                // 评论系统配置
    "enabled": true,           // 是否启用
    "require_approval": false  // 是否需要审核
  }
}
```

## 🔧 API 接口

| 接口 | 方法 | 说明 |
|------|------|------|
| `/api/config` | GET | 获取站点配置 |
| `/api/projects` | GET | 获取项目列表 |
| `/api/projects/<path>` | GET | 获取项目文档 |
| `/api/news` | GET | 获取咨询列表（按日期分组） |
| `/api/news/<path>` | GET | 获取咨询文档 |
| `/api/comments` | GET | 获取评论列表 |
| `/api/comments` | POST | 发表评论 |

## 🎨 主题定制

修改 `config.json` 中的 `theme` 部分：

```json
{
  "theme": {
    "primary_color": "#2563eb",      // 主色调
    "secondary_color": "#1e40af",    // 次色调
    "background_color": "#f8fafc",   // 背景色
    "card_background": "#ffffff",    // 卡片背景
    "text_color": "#1f2937",         // 文字颜色
    "text_secondary": "#6b7280"      // 次要文字
  }
}
```

## 📱 响应式断点

- **Desktop**: > 768px
- **Mobile**: ≤ 768px
- **Small Mobile**: ≤ 480px

## 🤝 贡献指南

欢迎提交 Issue 和 Pull Request！

## 📄 许可证

MIT License

## 🙏 致谢

- [Flask](https://flask.palletsprojects.com/) - Web框架
- [Marked](https://marked.js.org/) - Markdown解析
- [Highlight.js](https://highlightjs.org/) - 代码高亮

---

**X聚合信息** - 让信息展示更简单
